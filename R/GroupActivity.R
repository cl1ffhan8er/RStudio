Matrix = matrix(c(67,23,29,13,32,32), ncol=2, nrow=3)

Group_7 = data.frame (
  names = c("Jao", "Ariosa", "Tan", "Garrote", "Perez"),
  age = c(19,19,19,20,24),
  year = c(2,2,2,2,4)
)

multiply_numbers = function(a,b) {a * b}

Matrix
0Group_7
multiply_numbers(10,10)
